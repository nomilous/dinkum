Client = require '../../lib/client/client'

describe 'Client', ->

    context 'session', -> 

        context 'authentication', ->
        context 'cookies', -> 

    context 'status', -> 

        context 'active', -> 
        context 'queue', -> 

    context 'core methods', -> 

        context 'GET', ->
        context 'HEAD', -> 
        context 'POST', -> 
        context 'PUT', ->
        context 'DELETE', ->

    context 'extended methods', ->

        context 'TRACE', ->
        context 'OPTIONS', ->
        context 'CONNECT', ->
        context 'PATCH', -> 

